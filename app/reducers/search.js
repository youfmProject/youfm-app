import Constants from '../lib/Constants';

const { SEARCH } = Constants;

const search = (state = {searchKeywork:'',tracks:[]}, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default search;