import Constants from '../lib/Constants';

const { NOW_PLAYING } = Constants;

const nowPlaying = (state = {nowPlaying:{},queue:[]}, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default nowPlaying;
